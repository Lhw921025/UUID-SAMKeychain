# Uncomment the next line to define a global platform for your project
platform :ios, '8.0'
# 忽略引入库的所有警告    
inhibit_all_warnings!

def common_pods
    pod 'SAMKeychain'
end

target 'UUID-SAMKeychain' do
    common_pods
end

